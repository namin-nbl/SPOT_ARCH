{
  "name": "arch-spot-mission-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "ARCH SPOT mission monitoring prototype based on the SPOT ARCH BRD",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check server-data.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
